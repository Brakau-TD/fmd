--- app/src/main/java/com/example/MainActivity.kt
+++ app/src/main/java/com/example/MainActivity.kt
@@ -488,43 +488,6 @@
                 }
             }
 
-            // QR Code Scanner Overlay dialog
-            if (showQRScanner) {
-                Dialog(
-                    onDismissRequest = { showQRScanner = false },
-                    properties = DialogProperties(usePlatformDefaultWidth = false)
-                ) {
-                    Box(modifier = Modifier.fillMaxSize()) {
-                        CameraQRScanner(onQRCodeScanned = { qrText ->
-                            viewModel.processPairingQR(qrText, context)
-                            showQRScanner = false
-                        })
-
-                        // Cancel Scan float button
-                        Box(
-                            modifier = Modifier
-                                .fillMaxSize()
-                                .padding(24.dp),
-                            contentAlignment = Alignment.BottomCenter
-                        ) {
-                            Button(
-                                onClick = { showQRScanner = false },
-                                colors = ButtonDefaults.buttonColors(containerColor = CyberBlack, contentColor = TextPrimary),
-                                shape = RoundedCornerShape(12.dp),
-                                modifier = Modifier
-                                    .fillMaxWidth()
-                                    .height(56.dp)
-                            ) {
-                                Icon(Icons.Outlined.Cancel, contentDescription = null, modifier = Modifier.size(24.dp))
-                                Spacer(modifier = Modifier.width(8.dp))
-                                Text("Cancel Scan", fontSize = 16.sp, fontWeight = FontWeight.Bold)
-                            }
-                        }
-                    }
-                }
-            }
-
             // Manual Pairing Setup Dialogue (Highly useful in Emulators)
             if (showManualPairDialog) {
